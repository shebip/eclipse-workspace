package in.vamsoft.training.corejava;

import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.nio.file.attribute.BasicFileAttributes;
import java.nio.file.attribute.DosFileAttributes;

public class FileAttributes {

	public static void main(String[] args) {
		/*Path file= Paths.get(uri);
		BasicFileAttributes attr=Files.readAttributes(file, type, options)
    
				
	  System.out.println("CreationTime" +attr.creationTime());
		System.out.println("LastrAccessTime" +attr.lastAccessTime());
		System.out.println("LostModifiedTime" +attr.lastModifiedTime());
		
		System.out.println("isDirectory" +attr.isDirectory());
		System.out.println("isOther" +attr.isOther());
		System.out.println("isRegularFile" +attr.isRegularFile());
		
		DosFileAttributes attributes=Files.readAttributes(file,DosFileAttributes.class);
		System.out.println("");
	*/
	}
}
